# ── NOTICE ──────────────────────────────────────────────────────────
# This file is NOT used when running the unified Docker stack.
# All environment variables are centralized in the ROOT .env file:
#
#   /path/to/PRD-Socpilots/.env.example  ← copy and fill this one
#
# Docker Compose passes variables directly to this container via the
# environment: block in docker-compose.yml.
# ────────────────────────────────────────────────────────────────────
