services:
  thehive-mcp:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - ../.env
    environment:
      - THEHIVE_URL=${THEHIVE_URL}/api
      - THEHIVE_API_KEY=${THEHIVE_API_KEY}
      - PORT=8080
    restart: unless-stopped
