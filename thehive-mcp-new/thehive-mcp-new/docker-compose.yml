services:
  thehive-mcp:
    build: .
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - THEHIVE_URL=${THEHIVE_URL}
      - THEHIVE_API_KEY=${THEHIVE_API_KEY}
      - PORT=${PORT:-8080}
    restart: unless-stopped
